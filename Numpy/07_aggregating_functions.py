import numpy as np

arr = np.array([20, 40, 50, 70])

print(np.sum(arr))
print(np.min(arr))
print(np.max(arr))
print(np.size(arr))
print(np.mean(arr))
# cumsum: cumulative sum
print(np.cumsum(arr))
print(np.cumprod(arr))